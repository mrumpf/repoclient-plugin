task deploy << {
    println 'Hello world!'

    def env = System.getenv()
    env.each{
      println it
    } 

    def repoclient_urls = env.get('repoclient_urls')
    repoclient_urls.each{
      println it
    }
}

// task downloadFile { 
//    ant.get(src: '<url>', dest: buildDir, verbose:true)  // any Gradle equivalent? 
//} 